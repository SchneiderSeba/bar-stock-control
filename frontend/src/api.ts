import type {DashboardData,Invoice,Product,Supplier} from './types'
async function request<T>(path:string,options?:RequestInit):Promise<T>{const r=await fetch(`/api${path}`,{headers:{'Content-Type':'application/json'},...options});if(!r.ok)throw new Error((await r.json().catch(()=>({message:'Request failed'}))).message||'Request failed');return r.json()}
export const api={dashboard:()=>request<DashboardData>('/dashboard'),products:()=>request<Product[]>('/products'),suppliers:()=>request<Supplier[]>('/suppliers'),invoices:()=>request<Invoice[]>('/invoices'),createProduct:(body:unknown)=>request<Product>('/products',{method:'POST',body:JSON.stringify(body)}),createSupplier:(body:unknown)=>request<Supplier>('/suppliers',{method:'POST',body:JSON.stringify(body)}),createInvoice:(body:unknown)=>request<Invoice>('/invoices',{method:'POST',body:JSON.stringify(body)}),adjustStock:(id:number,body:unknown)=>request<Product>(`/products/${id}/adjust`,{method:'POST',body:JSON.stringify(body)})}

